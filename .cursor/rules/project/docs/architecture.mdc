---
description: System architecture documentation including pipeline flows, component relationships, deployment topology, and key design decisions. Use when making architectural decisions, understanding system flow, or modifying pipeline components.
alwaysApply: false
---

# System Architecture

## Overview

ElectricalDiagramAnalyzer is a Python backend that processes electrical PDF drawings (panel schedules, riser diagrams, one-line diagrams). It detects panel tables via heuristic whitespace/void analysis, extracts structured data (panel name, amperage, voltage, breakers) via OCR, and recommends equipment part numbers through a rules engine. The backend connects to an Anvil web UI via Uplink RPC.

## Component Diagram

```
PDF Upload (Anvil UI)
    │
    ▼
AnvilUplinkCode/uplink_server.py ── vm_submit_for_detection()
    │
    ├─► PageFilter (PageFilterV3)
    │       │  keeps electrical pages, outputs filtered PDF
    │       ▼
    ├─► PanelBoardSearch (PanelSearchToolV25)
    │       │  detects panel regions, crops to PNGs
    │       ▼
    ├─► BreakerTablePipeline (BreakerTableParserAPIv9)
    │       ├─► BreakerTableAnalyzer (Analyzer12)
    │       │       ├── BreakerHeaderFinder
    │       │       └── BreakerFooterFinder
    │       ├─► PanelHeaderParser (PanelHeaderParserV9)
    │       └─► BreakerTableParser (BreakerTableParser10)
    │       │  extracts structured OCR data per panel
    │       ▼
    ├─► _merge_component_from_btp()
    │       │  maps OCR results to component dicts
    │       ▼
    └─► RulesEngine4.process_job()
            └── PartNumberBuilder (mccb, nqPanelboard, iLinePanelboard,
            │     loadcenter, Disconnect, transformer, externalSpd, ...)
            │  selects equipment part numbers
            ▼
        result.json + status.json on disk
```

## Key Components

### AnvilUplinkCode (Orchestrator)
- **Purpose**: Anvil Uplink server; exposes RPC callables, manages job queue, multi-slot persistent worker pool (4 slots), watchdog, and worker recycling
- **Location**: `AnvilUplinkCode/uplink_server.py`
- **Dependencies**: WorkerSetup, PageFilter, PanelBoardSearch, BreakerTablePipeline, RulesEngine4
- **Consumers**: Anvil web frontend via Uplink RPC

### PageFilter
- **Purpose**: Filters PDF pages to keep only electrical/panel schedule pages using OCR and heuristics
- **Location**: `PageFilter/PageFilterV3.py`
- **Dependencies**: pypdfium2, pikepdf, EasyOCR, OpenCV
- **Consumers**: uplink_server (pre-processing step)

### VisualDetectionToolLibrary (Panel Detection)
- **Purpose**: Detects panel table regions in PDF bitmaps using whitespace/void analysis and border detection
- **Location**: `VisualDetectionToolLibrary/PanelSearchToolV25.py`
- **Dependencies**: pypdfium2, pikepdf, OpenCV, NumPy, PIL
- **Consumers**: uplink_server (panel crop step)

### OcrLibrary (OCR Pipeline)
- **Purpose**: Three-stage OCR pipeline -- layout analysis, header parsing, breaker table parsing -- plus shared OCR timeout utility
- **Location**: `OcrLibrary/` (BreakerTableParserAPIv9, BreakerTableAnalyzer12, PanelHeaderParserV9, BreakerTableParser10, ocr_timeout)
- **Dependencies**: EasyOCR, OpenCV, NumPy, AnchoringClasses
- **Consumers**: uplink_server via BreakerTablePipeline

### AnchoringClasses
- **Purpose**: Structural layout detection -- finds header and footer anchor positions in panel images
- **Location**: `AnchoringClasses/BreakerHeaderFinder.py`, `AnchoringClasses/BreakerFooterFinder.py`
- **Dependencies**: EasyOCR reader (shared), OpenCV, NumPy, `OcrLibrary.ocr_timeout` (header finder only)
- **Consumers**: BreakerTableAnalyzer12

### RulesEngine
- **Purpose**: Processes component dicts from OCR to select equipment part numbers via engine classes
- **Location**: `RulesEngine/RulesEngine4.py`
- **Key classes**: `DefaultResolver`, `BaseEngine`, `PanelboardEngine`, `DisconnectEngine`, `TransformerEngine`
- **Dependencies**: PartNumberSelector
- **Consumers**: uplink_server (final pipeline step)

### PartNumberSelector
- **Purpose**: Part number lookup and selection logic for panelboards, load centers, breakers, disconnects, transformers, external SPDs
- **Location**: `PartNumberSelector/PartNumberBuilder.py`
- **Exports**: `mccb`, `nqPanelboard`, `nfPanelboard`, `iLinePanelboard`, `loadcenter`, `breakerSelector`, `blanks`, `eFrameBreaker`, `Disconnect`, `transformer`, `externalSpd`
- **Dependencies**: None (pure logic)
- **Consumers**: RulesEngine4

### WorkerSetup
- **Purpose**: Thread-pool caps (`OMP_NUM_THREADS`, `MKL_NUM_THREADS`, etc.) and runtime determinism (`torch.set_num_threads(1)`, `cv2.setNumThreads(1)`, cuDNN flags) for multiprocessing workers
- **Location**: `WorkerSetup/worker_init.py`
- **Dependencies**: None (uses only stdlib `os`; optional imports of `cv2` and `torch`)
- **Consumers**: `uplink_server.py` (module-level import; each worker subprocess re-imports it)

### OCR Timeout (Shared)
- **Purpose**: Wraps `reader.readtext()` in a daemon thread with a configurable wall-clock timeout; returns `[]` on timeout instead of blocking
- **Location**: `OcrLibrary/ocr_timeout.py`
- **Default timeout**: 30 seconds (`OCR_TIMEOUT_SEC`)
- **Consumers**: `BreakerTableParser10`, `PanelHeaderParserV9`, `BreakerHeaderFinder`

## Worker Architecture

The server uses a **multi-slot persistent worker pool**:

```
Main process (Anvil Uplink)
    │
    ├── _WorkerSlot[0..3]   (4 slots, one per MAX_WORKERS)
    │       │
    │       ├── _spawn_persistent_worker(idx)
    │       │       │  spawns long-lived child via multiprocessing.get_context("spawn")
    │       │       │  child loads GPU EasyOCR reader + BreakerTablePipeline once
    │       │       │  signals "ready" via slot.done_q
    │       │       ▼
    │       │   _persistent_worker_main(slot_idx, job_q, done_q)
    │       │       │  loops: job_q.get() → reset_name_deduper → _process_job → done_q.put()
    │       │       │  between-job cleanup: gc.collect() + torch.cuda.empty_cache()
    │       │       │  auto-recycles after WORKER_RECYCLE_AFTER_JOBS (25) jobs
    │       │       │  pipeline (with GPU reader) reused across all jobs within lifecycle
    │       │
    │       └── _dequeue_loop(idx)   [daemon thread, one per slot]
    │               │  pulls (job_id, owner_id) from shared _JOB_Q
    │               │  checks: already done/error/canceled? → skip
    │               │  checks: queue timeout? → write error, skip
    │               │  checks: per-user inflight cap → re-enqueue if full
    │               │  acquires slot.lock → _ensure_worker_alive() → auto-respawn if needed
    │               │  puts job_id on slot.job_q
    │               │  waits on slot.done_q with watchdog timeout
    │               │  on timeout/crash: write error, kill worker, respawn
    │
    └── anvil.server.wait_forever()
```

Key aspects:
- **Multi-slot pool**: `MAX_WORKERS=4` independent worker slots, each with its own subprocess, queues, and dequeue thread
- **Per-slot isolation**: Each `_WorkerSlot` has a `lock`, `proc`, `job_q`, and `done_q` -- no cross-slot queue access
- **`_IS_WORKER_SUBPROCESS`** env var (`_EDA_WORKER_SUBPROCESS=1`) prevents child processes from connecting to Anvil, starting their own workers, or running warmup
- **GPU model loading**: Each worker subprocess loads its own GPU EasyOCR reader independently; the main process skips OCR warmup to conserve VRAM
- **Runtime determinism**: `_set_runtime_determinism()` runs at module level (line 89); worker subprocesses inherit this via the `spawn` context re-import -- the worker does NOT call it again to avoid a PyTorch `RuntimeError`
- **Between-job cleanup**: After every job, `gc.collect()` and `torch.cuda.empty_cache()` run to mitigate memory degradation
- **Worker recycling**: After `WORKER_RECYCLE_AFTER_JOBS` (default 25) jobs, the worker exits cleanly; `_ensure_worker_alive()` detects the exit and respawns
- **`_WORKER_READY_TIMEOUT = 120`** seconds to wait for each worker to finish loading models
- **Watchdog**: If `slot.done_q.get()` times out after `WATCHDOG_TIMEOUT_MIN * 60` seconds, the job is marked as error, the worker is killed and respawned
- **Per-user concurrency**: `_enter_inflight` / `_leave_inflight` enforce `MAX_INFLIGHT_PER_USER=1` -- jobs exceeding the cap are re-enqueued after a short delay
- **Spawn serialization**: `_SPAWN_LOCK` ensures env-var set/start/unset is atomic across slots when spawning workers

## Data Pipelines

### Main Detection Pipeline
- **Trigger**: `vm_submit_for_detection()` callable from Anvil UI
- **Steps**:
  1. Save uploaded PDF to `~/jobs/{job_id}/uploaded_pdfs/`
  2. Write initial `status.json` (state=queued, owner_id, noticed_ts_ms, ui_overrides)
  3. Enqueue `(job_id, owner_id)` to `_JOB_Q`
  4. One of the 4 dequeue-loop threads picks up the job
  5. Queue timeout check (if age exceeds `QUEUE_TIMEOUT_MIN`, write error and skip)
  6. Per-user inflight check (if user already has a job running, re-enqueue)
  7. Dispatch `job_id` to the slot's persistent worker subprocess via `slot.job_q`
  8. Worker calls `_process_job(job_id, pipeline)`:
     a. `PageFilter.readPdf()` filters pages
     b. `PanelBoardSearch.readPdf()` detects and crops panels to PNGs
     c. For each PNG (sequential): `BreakerTablePipeline.run()` extracts OCR data
     d. `_merge_component_from_btp()` maps each OCR result to a component dict
     e. `RulesEngine4.process_job()` selects part numbers for all components
     f. Write `result.json` and update `status.json` (state=done)
     g. `_cleanup_job_dir()` removes temporary files, keeps overlays and PDFs
  9. Worker signals completion via `slot.done_q`
  10. Between-job cleanup: `gc.collect()` + `torch.cuda.empty_cache()`
- **Output**: `result.json` with components, part numbers, and cycle time
- **Error handling**: Exceptions write state=error to `status.json`; watchdog kills and respawns worker on timeout

## Deployment Topology

- **Runtime**: Single Paperspace VM
- **Connection**: Anvil Uplink RPC (`anvil.server.connect()` with `ANVIL_UPLINK_KEY`)
- **Concurrency**: 4 dequeue-loop daemon threads dispatching to 4 persistent worker subprocesses (one per `_WorkerSlot`)
- **Per-user limit**: 1 concurrent job per user (`MAX_INFLIGHT_PER_USER=1`)
- **Worker recycling**: Each worker auto-recycles after 25 jobs (`WORKER_RECYCLE_AFTER_JOBS`); `_ensure_worker_alive()` respawns transparently
- **Watchdog**: Kills worker process if job exceeds `WATCHDOG_TIMEOUT_MIN` (default 10 minutes); auto-respawns
- **Queue timeout**: Jobs waiting longer than `QUEUE_TIMEOUT_MIN` (default 25 minutes) are expired with error
- **Job storage**: `~/jobs/{job_id}/` with status.json, result.json, PDF, and PNG files
- **No containerization**: Runs directly on the VM with system Python

## Key Design Decisions

### Multi-Slot Persistent Worker Pool
- **Decision**: 4 independent worker subprocesses (`MAX_WORKERS=4`), each loading GPU models once and processing jobs sequentially within its lifecycle
- **Context**: EasyOCR + PyTorch GPU model loading takes ~60-120s. Loading per-job was wasteful. Each worker loads a GPU reader once at startup, creates a `BreakerTablePipeline`, and reuses it across all assigned jobs. With 4 slots, up to 4 jobs can be processed in parallel (limited by per-user cap).
- **Alternatives considered**: Single persistent worker (bottleneck under concurrent users), per-job subprocess spawning (too slow for model reload), in-process execution (state leaks between runs)
- **Consequences**: Higher throughput via parallelism; each slot is a single point of failure mitigated by auto-respawn via `_ensure_worker_alive()`

### Worker Recycling
- **Decision**: Workers auto-exit after `WORKER_RECYCLE_AFTER_JOBS` (default 25) jobs; the dequeue loop respawns them
- **Context**: Long-lived Python processes accumulate RSS (pymalloc doesn't return memory to OS), leaked OCR timeout daemon threads, and fragmented GPU memory. Periodic recycling reclaims all of this with a clean subprocess.
- **Alternatives considered**: In-process cleanup only (insufficient for leaked C-level threads and pymalloc behavior)
- **Consequences**: Brief respawn overhead (~60-120s every 25 jobs per slot); combined with `gc.collect()` + `torch.cuda.empty_cache()` between every job for mid-lifecycle cleanup

### Process-Based Job Isolation
- **Decision**: Each job runs in a persistent worker subprocess, not in the main thread
- **Context**: EasyOCR, PyTorch, and OpenCV have state that leaks across runs; subprocess ensures clean state
- **Alternatives considered**: Thread-based execution (caused non-deterministic OCR results)
- **Consequences**: Pipeline instance is reused within a recycling window but `reset_name_deduper()` is called between jobs

### Thread-Pool Capping for Multiprocessing
- **Decision**: Cap OpenMP/MKL/OpenBLAS thread pools to 1 thread per process and apply PyTorch/OpenCV runtime determinism
- **Context**: EasyOCR and PyTorch create internal thread pools during library init. Competing thread pools deadlock at the C level. `cap_thread_pools()` runs before heavy imports at module level; `set_runtime_determinism()` runs once at module level and is inherited by worker subprocesses via the `spawn` context.
- **Consequences**: Slightly slower single-process math operations, but no deadlocks. Centralised in `WorkerSetup/worker_init.py`.

### Queue Timeout
- **Decision**: Jobs that wait in the queue longer than `QUEUE_TIMEOUT_MIN` (default 25 min) are expired
- **Context**: Under load, jobs could queue indefinitely; timeout gives users a clear failure message and frees the queue
- **Consequences**: Users must resubmit if their job expires; configurable at runtime via `vm_set_queue_timeout`

### Versioned Module Files
- **Decision**: Modules carry version suffixes (e.g., PageFilterV3, PanelSearchToolV25, BreakerTableAnalyzer12)
- **Context**: Enables side-by-side comparison during development; highest number is the active version
- **Alternatives considered**: Git branching only (harder to compare in dev scripts)
- **Consequences**: Multiple version files in each directory; only the highest version is imported by uplink_server
